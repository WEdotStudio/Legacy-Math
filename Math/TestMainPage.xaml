<Page
    x:Class="Math.TestMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Math"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Peru">

    <Grid>
        <Grid Background="White">
            <Hub Header="Mαth" Foreground="Black" SectionHeaderClick="Hub_SectionHeaderClick">
                <HubSection x:Name="Main" Header="Mαin" Width="500" IsHeaderInteractive="True">
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                        </Grid>
                    </DataTemplate>
                </HubSection>
                <HubSection x:Name="calc" Header="Cαlc" Width="500" IsHeaderInteractive="True">
                </HubSection>
                <HubSection x:Name="Formu" Header="Formulα" Width="500" IsHeaderInteractive="True">
                </HubSection>
                <HubSection x:Name="about" Header="αbout" Width="500" IsHeaderInteractive="True">
                </HubSection>

            </Hub>
        </Grid>
    </Grid>
</Page>
